// besq-core placeholder translation unit.
//
// besq-core is a STATIC aggregate of the three non-interface domains plus the
// embedded data group (data_assets.cpp, generated from the
// besq_embed_resources() declaration in CMakeLists.txt — compiled into
// besq-domain-business next to its loader).  CMake rejects a target without
// sources, so this empty TU anchors the library; all real code lives in the
// linked domain libraries.
