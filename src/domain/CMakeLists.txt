# ── Domain parent ──────────────────────────────────────────────────────────
# Adds the three domain subdirectories.  Each produces a static library.
add_subdirectory(algorithm)
add_subdirectory(business)
add_subdirectory(interface)
